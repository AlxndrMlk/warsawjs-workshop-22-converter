// Phi = (Math.sqrt(5) + 1) / 2
// phi = phi - 1

const examples = {
    add: (a, b) => {
      return a + b
     },

    hello: (name) => {
      return `hello ${name}`}
    //  ,
    //
    // fib: (i) => {
    //   if (i === 1 || i===2) {
    //     return 1
    //     } else {
    //     return Phi**n - (-Phi)**(-n)
    //       }

    }


export default examples
